<%= form_for(@dashboard_message) do |f| %>
  <% if @dashboard_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dashboard_message.errors.count, "error") %> prohibited this dashboard_message from being saved:</h2>

      <ul>
      <% @dashboard_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
