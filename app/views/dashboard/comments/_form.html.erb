<%= form_for(@dashboard_comment) do |f| %>
  <% if @dashboard_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dashboard_comment.errors.count, "error") %> prohibited this dashboard_comment from being saved:</h2>

      <ul>
      <% @dashboard_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
