<h1>New blog</h1>

<%= dashboard_new_blog_path %>
and
<%= dashboard_new_blog_post_path %>

<%= form_for @blog, url: dashboard_new_blog_post_path do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :subdomain %><br>
    <%= f.text_field :subdomain %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
